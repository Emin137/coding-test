#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
	int a, b;
	cin >> a >> b;
	a = a * -b;
	int c, d, e, f, g;
	cin >> c >> d >> e >> f >> g;
	cout << c + a << ' ' << d + a << ' ' << e + a << ' ' << f + a << ' ' << g + a;
	
}
	


